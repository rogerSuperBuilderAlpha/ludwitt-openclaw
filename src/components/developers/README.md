# Developer Components — V1 / V2 Split

The developer dashboard uses **two component sets intentionally**:

- **`v1/`** — Main developer dashboard layout and overview pages
- **`v2/`** — Document detail views, modals, and sub-pages

Both are actively imported. This is not dead code — they serve different parts of the developer experience. Consolidation is tracked but not yet scheduled.
