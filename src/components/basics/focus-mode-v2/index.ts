/**
 * Focus Mode V2 Components
 *
 * Redesigned immersive focus mode for math practice with:
 * - Full-screen layout
 * - Problem + visualization on left
 * - Answer input on right (stacked on mobile)
 * - Session progress tracking
 * - Streak indicators
 * - 3x XP multiplier display
 */

export { MathFocusV2 } from './MathFocusV2'

export { FocusModeModalV2 } from './FocusModeModalV2'

export { SessionProgress } from './SessionProgress'
