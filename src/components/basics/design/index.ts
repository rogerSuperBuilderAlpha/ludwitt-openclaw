/**
 * Basics Design System
 *
 * Unified export for all design system components and utilities.
 */

export { BasicsCard, CardHeader, CardIcon, SectionWrapper } from './BasicsCard'
export type { CardVariant, SubjectAccent } from './BasicsCard'

// Re-export tokens for convenience
export * from '@/lib/design/tokens'
